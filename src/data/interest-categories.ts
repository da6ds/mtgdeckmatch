// src/data/interest-categories.ts

export interface InterestCategory {
  id: string;
  slug: string;
  name: string;
  description: string;
  artUrl: string;
  // Additional fields will be added during implementation
}

// Placeholder categories - will be populated with 12 categories
export const interestCategories: InterestCategory[] = [
  // Categories to be defined in implementation
];
